# Текущий список задач

## Текущая волна

Все задачи, которые делались в этой волне, выполнены.

### Выполнено

- Переведена логика машин на `GameplayKit`.
- Переведена логика соседей на `GameplayKit`.
- Переведены двери и проходы на `GameplayKit`.
- Переведены режимы движения комнат и двора на `GameplayKit`.
- Переведены поза игрока и инвентарь на `GameplayKit`.
- Переведены предметы с этапами на `GameplayKit`:
  телевизор, стол, зеркало, бутылка у холодильника, подушка и кровать.
- Проведена полная чистка мертвых хвостов после миграции на `GameplayKit`.
- Ручные проверки через `MCP` для этих задач пройдены.

### Открытые задачи

- На текущую волну открытых задач нет.
